{
  "name": "formstate-inator",
  "version": "0.0.60",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "watch": "npm run test -- --watch-extensions ts --watch -R min",
    "build": "tsc -d -p ."
  },
  "dependencies": {
    "io-ts": "^1.10.2",
    "io-ts-derive-class": "^0.0.30",
    "validator-inator": "^0.0.32",
    "mobx": "^5.10.1"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2",
    "date-fns": "^1.30.1"
  }
}
